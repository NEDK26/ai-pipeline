# F-001: Requirements Review — 演示配置开关

**Review ID**: RR-F-001-001
**Feature ID**: F-001
**Spec Version**: 0.1
**Review Date**: 2026-07-06
**Reviewer**: requirements-reviewer
**Conclusion**: **Ready**

---

## 审查范围

| 文件 | 审查 |
|------|------|
| `brief.md` | ✅ |
| `spec.md` | ✅ |
| `prototype.md` | ✅ |
| `api-contract.md` | ✅ |
| `acceptance-tests.md` | ✅ |
| `architecture-impact.md` | ✅ |
| `handoff.md` | ✅ |

---

## 逐项审查

### 1. 角色权限完整性

| 检查点 | 结果 |
|--------|------|
| ConfigAdmin 读写权限定义 | ✅ `system:demo-mode:query` + `system:demo-mode:update` |
| Operator 只读定义 | ✅ 仅 `system:demo-mode:query` |
| Operator 修改 API 拒绝 | ✅ BR-005 + api-contract PUT 403 + AT-010 |
| 无权限用户拒绝 | ✅ BR-005 + api-contract GET/PUT 403 + AT-013 |
| 前端入口保护 | ✅ v-hasPermi 移除按钮/页面 |
| 权限字符串命名符合项目约定 (`module:entity:action`) | ✅ |

**结论**: 通过。Operator 直接调 API 修改的拒绝路径在 spec、契约、验收三层均有落点。

### 2. 修改原因、审计字段、幂等规则明确性

| 检查点 | 结果 |
|--------|------|
| 原因必填 + 长度限制 | ✅ BR-002: `@NotBlank` + `@Size(min=4, max=200)` |
| 审计字段完整性（操作人/时间/旧值/新值/原因） | ✅ BR-003: type/subType/action/extra JSON |
| 审计存储位置明确 | ✅ `system_operate_log` 表，复用现有框架 |
| 幂等判断规则可执行 | ✅ BR-004: 字符串比较，无时间窗口 |
| 幂等行为定义完整（不写库、不审计） | ✅ BR-004 + api-contract 幂等章节 |
| 错误码与触发条件一一对应 | ✅ 3 个业务错误码 |

**结论**: 通过。所有规则均可直接转化为代码，无模糊措辞。

### 3. API 契约可实现性

| 检查点 | 结果 |
|--------|------|
| 路径与方法明确 | ✅ GET `/system/demo-mode/get`, PUT `/system/demo-mode/update` |
| 请求参数字段、类型、校验完整 | ✅ |
| 响应字段类型与 null 语义明确 | ✅ |
| 成功/幂等响应区分清晰 | ✅ |
| 业务错误码与 HTTP 状态码映射完整 | ✅ |
| 异常响应 (401/403/500) 定义 | ✅ |
| 幂等规则独立章节 | ✅ |

**附注**: 错误码前缀 `1_001_001_xxx` 需在实现时确认与 `exrobot-module-infra` 模块已有错误码范围不冲突。此为实现细节，不阻塞开发。

**结论**: 通过。契约可直接作为后端实现和前端 mock 的基准。

### 4. 验收用例覆盖

| 覆盖类别 | 用例 | 状态 |
|---------|------|------|
| 正常查询 | AT-001, AT-002 | ✅ |
| 正常修改 | AT-003 | ✅ |
| 审计写入验证 | AT-004, AT-009 | ✅ |
| 幂等（重复提交） | AT-005 | ✅ |
| 参数校验（缺字段/短原因） | AT-006, AT-007, AT-008 | ✅ |
| 权限拒绝（Operator 改、noperm 查/改） | AT-010, AT-011, AT-013 | ✅ |
| 前端二次确认（确认/取消） | AT-014, AT-015 | ✅ |
| 角色视图差异 | AT-016, AT-017 | ✅ |
| 前端表单校验 | AT-018 | ✅ |
| 边界值（200/201 字符） | AT-019, AT-020 | ✅ |

**结论**: 通过。20 条用例覆盖 6 条业务规则 + 3 条用户故事。每条规则均有可执行、可观察的验证步骤。

### 5. 非目标

| 非目标 | 是否明确 | 文档位置 |
|--------|---------|---------|
| 批量修改 | ✅ | brief.md, spec.md 范围 |
| 配置分组 | ✅ | brief.md |
| 定时生效 | ✅ | brief.md |
| DemoFilter 修改 | ✅ | brief.md + architecture-impact.md 未修改清单 |
| 环境特定默认值 | ✅ | brief.md |

**结论**: 通过。5 项非目标均有明确声明，防止范围蔓延。

### 6. 阻塞开发的缺失信息或冲突

| 检查项 | 状态 |
|--------|------|
| 信息冲突 | 无 |
| 缺失的关键业务决策 | 无 |
| 无法落地的架构约束 | 无 |
| 不可测试的验收条件 | 无 |

**O-001 (ConfigAdmin 角色是否存在)** 为运维配置问题，不影响后端代码实现——实现者按权限字符串注册即可，角色不存在时创建。不构成阻塞。

**结论**: 无阻塞项。

---

## Quality Gate (9 项)

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 是否把猜测写成事实 | ✅ 通过：Confirmed/Assumption/Open Question 已区分 |
| 2 | 是否存在无法测试的业务规则 | ✅ 通过：6 条规则均有验收用例 |
| 3 | 是否存在没有验收用例的用户故事 | ✅ 通过：3 故事 → 20 用例 |
| 4 | 是否存在没有页面或接口落点的关键流程 | ✅ 通过 |
| 5 | 是否存在没有权限定义的高风险操作 | ✅ 通过（无高风险操作，所有操作有权限） |
| 6 | 是否存在没有失败处理的状态转换 | N/A（无状态机） |
| 7 | 是否存在没有来源或口径的数据指标 | ✅ 通过 |
| 8 | 是否存在被遗漏的架构约束 | ✅ 通过 |
| 9 | Loop B 是否仍需要猜测关键业务决策 | ✅ 通过 |

---

## 审查结论

**Ready** — 规格包完整、可执行、无阻塞项。O-001 为运维配置问题，实现时按需处理即可，不阻塞 Loop B。

### 建议（非阻塞）

1. 实现时确认 `1_001_001_xxx` 错误码段在 `exrobot-module-infra` 中不冲突
2. `reason` 字段 200 字符上限如有变更，同步更新 api-contract、spec 和前端 maxlength

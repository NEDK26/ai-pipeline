# $feature_id：API 契约

> 后端按此契约实现接口，前端按此契约用 mock 或真实接口开发，验证用验收用例检查前后端行为一致性。

## 接口清单

| 编号 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|

---

## API-001：<接口名称>

### 基本信息

| 项 | 值 |
|----|-----|
| 方法 | GET / POST / PUT / DELETE |
| 路径 | `/admin-api/xxx` |
| 说明 | 一句话说明接口职责 |
| 权限 | 所需角色或权限标识 |
| 状态限制 | 调用前资源必须处于的状态 |

### 请求参数

**Path 参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|

**Query 参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|

**Body 参数**

| 参数 | 类型 | 必填 | 校验规则 | 说明 |
|------|------|------|----------|------|

### 请求示例

```json
{
}
```

### 成功响应

**状态码**: `200`

```json
{
  "code": 0,
  "msg": "success",
  "data": {
  }
}
```

**返回字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|

### 业务错误码

| 错误码 | 说明 | 触发条件 | 用户提示 |
|--------|------|----------|----------|

### 异常响应

| 状态码 | 场景 | 响应示例 |
|--------|------|----------|
| 400 | 参数校验失败 | |
| 401 | 未登录 | |
| 403 | 无权限 | |
| 404 | 资源不存在 | |
| 409 | 状态不允许 / 冲突 | |
| 500 | 服务异常 | |

### 分页 / 排序 / 筛选规则

- 分页：`pageNo`（默认 1）、`pageSize`（默认 10，最大 100）
- 排序：支持字段、默认排序
- 筛选：支持字段、多选/单选、模糊/精确

### 幂等规则

- 幂等键：
- 重复提交行为：
- 幂等有效期：

### 前后端对接说明

| 角色 | 依赖项 | 说明 |
|------|--------|------|
| 后端 | 本契约 | 按字段、校验规则、错误码实现 |
| 前端 | 本契约 | 开发阶段可用 mock 数据，联调时切换到真实接口 |
| 验证 | `acceptance-tests.md` | 验收用例覆盖正常、异常、权限、状态、边界场景 |

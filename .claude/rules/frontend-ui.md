---
paths:
  - "RSP/rsp-vue/src/**/*.{vue,ts,css,scss}"
---

# Enterprise UI Rules

- 新页面先检查现有 Design Token、组件库和页面模式。
- 同类状态使用统一 Badge、Tag、Alert 组件，不重复造样式。
- 表格页必须提供：筛选、重置、分页、空状态、加载状态。
- 表单页必须提供：必填标识、校验提示、提交中状态、防重复提交。
- 删除、发布、回滚、强制更新等高风险操作必须二次确认。
- 不新增装饰性渐变、玻璃背景、发光阴影。
- 不要为了“好看”降低表格、列表和表单的信息密度。